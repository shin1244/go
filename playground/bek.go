package playground

import "fmt"

func solution(numbers []int, target int) int {
    arr := []int{0}
    
    for _, v := range number {
        temp := []
        for arr {
            n := arr[0]
            arr = arr[1:]
            temp = append(temp, v, -v...)
        }
    }
    return 0
}